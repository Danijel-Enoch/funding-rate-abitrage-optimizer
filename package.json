{
  "name": "basis-trading-backtest",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun run src/tui.ts",
    "tui": "bun run src/tui.ts",
    "backtest": "bun run src/index.ts",
    "optimize": "bun run src/index.ts --optimize",
    "markets": "bun run src/index.ts --markets",
    "list": "bun run src/index.ts --list",
    "correlation": "bun run src/correlation.ts",
    "scan": "bun run src/scanner.ts",
    "optimize-markets": "bun run src/optimize-markets.ts",
    "basket": "bun run src/optimize-basket.ts",
    "stocks": "bun run src/optimize-stocks.ts",
    "api": "cd api && bun run dev",
    "ui": "cd ui && npm run dev",
    "dev": "concurrently \"bun run api\" \"bun run ui\""
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
